import React from "react";

const Card = ({ title, description }) => {
  return (
    <div>
      <h2>Título: {title}</h2>
      <p>Descripción: {description}</p>
    </div>
  );
};

export default Card; //karen 
